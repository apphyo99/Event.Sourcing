# RabbitMQ development configuration

# Allow guest user from non-localhost (needed inside Docker network)
loopback_users.guest = false

# Logging
log.console = true
log.console.level = info

# Management plugin
management.tcp.port = 15672

# Default vhost is set via RABBITMQ_DEFAULT_VHOST env var in docker-compose
